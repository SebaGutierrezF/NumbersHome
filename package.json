{
  "name": "number-home",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node backend/server.js",
    "dev:backend": "nodemon backend/server.js",
    "dev:frontend": "cd docs && npm run dev",
    "build": "cd docs && npm run build",
    "preview": "cd docs && npm run preview",
    "install:all": "npm install && cd backend && npm install && cd ../docs && npm install"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "firebase": "^10.14.1",
    "leaflet": "^1.9.4"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
